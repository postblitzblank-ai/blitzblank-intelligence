import { ModulStub } from "@/components/modul-stub";

export default function AnalysePage() {
  return (
    <ModulStub
      titel="Unternehmensanalyse"
      untertitel="Reine Auswertungsschicht — Empfehlungen als Satz, nicht als Rohdaten-Tabelle."
      hinweis="Liefert erst mit wachsender Datenmenge Erkenntnisse (Antwortquoten, Vorlagen-Wirkung, Regionen-Vergleich)."
    />
  );
}
