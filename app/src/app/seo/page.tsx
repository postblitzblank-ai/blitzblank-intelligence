import { ModulStub } from "@/components/modul-stub";

export default function SeoPage() {
  return (
    <ModulStub
      titel="SEO-Center"
      untertitel="Website Health, Rankings, Search Console, technische Fehler — Tiefe in der Engine, nicht auf dem Bildschirm."
      hinweis="Die Search-Console- und PageSpeed-Anbindung folgt, sobald die Google-Rechteübertragung abgeschlossen ist."
    />
  );
}
