import { ModulStub } from "@/components/modul-stub";

export default function DirektkundenPage() {
  return (
    <ModulStub
      titel="Direktkunden"
      untertitel="Neue Auftraggeber gewinnen — eingehend und ausgehend, eine gemeinsame Firmenakte."
      hinweis="Firmenakten, KI-Funde und der Sammel-Freigabe-Workflow entstehen in den nächsten Schritten."
    />
  );
}
